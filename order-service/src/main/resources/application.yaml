spring:
  application:
    name: order-service

  datasource:
    url: jdbc:postgresql://localhost:8081/orders
    username: postgres
    password: OMKAR.04XX
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8085